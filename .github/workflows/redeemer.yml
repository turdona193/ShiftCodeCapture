name: Redeem Gearbox Shift Codes

on:
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest

    steps:
      - name: Run inline shell commands
        run: |
          echo "Hello World"
          ls -la